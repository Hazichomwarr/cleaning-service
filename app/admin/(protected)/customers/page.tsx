import Link from "next/link";
import { parseAdminCustomersQuery, type AdminCustomersSearchParams } from "@/src/lib/admin-customers-query";
import { getAdminCustomers } from "@/src/services/admin-customers.service";

function pageHref(query: ReturnType<typeof parseAdminCustomersQuery>, page: number) { const params = new URLSearchParams(); if (query.search) params.set("search", query.search); if (page > 1) params.set("page", String(page)); const value = params.toString(); return value ? `/admin/customers?${value}` : "/admin/customers"; }
function Contact({ email, phone }: { email: string | null; phone: string | null }) { return <div className="space-y-1 text-sm text-slate-600">{phone ? <p>{phone}</p> : null}{email ? <p className="break-all">{email}</p> : null}</div>; }
function Empty({ search }: { search: string }) { return <div className="rounded-2xl border border-dashed border-slate-300 bg-white px-6 py-12 text-center"><h2 className="text-lg font-semibold text-slate-950">{search ? `No customers found for “${search}”.` : "No customers saved yet."}</h2><p className="mx-auto mt-2 max-w-md text-sm leading-6 text-slate-600">{search ? "Try a different name, email, or phone number." : "Add existing customers so their contact and property information can be reused later."}</p><Link href={search ? "/admin/customers" : "/admin/customers/new"} className="mt-5 inline-flex min-h-10 items-center rounded-xl border border-slate-300 px-4 text-sm font-semibold text-slate-700">{search ? "Clear search" : "Add customer"}</Link></div>; }
export default async function CustomersPage({ searchParams }: { searchParams: Promise<AdminCustomersSearchParams> }) { const query = parseAdminCustomersQuery(await searchParams); const result = await getAdminCustomers(query); return <main className="mx-auto max-w-7xl"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm font-bold uppercase tracking-[0.16em] text-blue-700">Customers</p><h1 className="mt-3 text-3xl font-semibold tracking-tight text-slate-950 sm:text-4xl">Customer workspace</h1><p className="mt-3 max-w-2xl text-base leading-7 text-slate-600">Keep customer contact details ready for future bookings.</p></div><Link href="/admin/customers/new" className="inline-flex min-h-11 items-center rounded-xl bg-blue-700 px-5 font-semibold text-white">Add customer</Link></div><form method="get" className="mt-8 flex flex-col gap-3 rounded-2xl border border-slate-200 bg-white p-4 shadow-sm sm:flex-row"><label htmlFor="customer-search" className="sr-only">Search customers</label><input id="customer-search" name="search" type="search" defaultValue={query.search} placeholder="Search name, email, or phone" className="min-h-11 min-w-0 flex-1 rounded-xl border border-slate-300 px-3 text-sm" /><button className="min-h-11 rounded-xl bg-slate-900 px-5 text-sm font-semibold text-white">Search</button>{query.search ? <Link href="/admin/customers" className="inline-flex min-h-11 items-center justify-center px-3 text-sm font-semibold text-blue-700">Clear</Link> : null}</form><p className="mt-5 text-sm text-slate-500"><span className="font-semibold text-slate-800">{result.pagination.totalItems}</span> {result.pagination.totalItems === 1 ? "customer" : "customers"}</p>{result.items.length === 0 ? <div className="mt-4"><Empty search={query.search} /></div> : <><div className="mt-4 hidden overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm md:block"><table className="w-full text-left"><caption className="sr-only">Customers</caption><thead className="border-b border-slate-200 bg-slate-50"><tr>{["Customer", "Contact", "Properties", "Requests", "Status", ""].map((heading) => <th key={heading} className="px-5 py-4 text-xs font-bold uppercase tracking-[0.12em] text-slate-500">{heading}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{result.items.map((item) => <tr key={item.id}><td className="px-5 py-5 font-semibold text-slate-950">{item.name}</td><td className="px-5 py-5"><Contact email={item.email} phone={item.phone} /></td><td className="px-5 py-5 text-sm text-slate-700">{item.propertyCount}</td><td className="px-5 py-5 text-sm text-slate-700">{item.requestCount}</td><td className="px-5 py-5 text-sm">{item.isActive ? <span className="font-semibold text-emerald-700">Active</span> : <span className="font-semibold text-slate-500">Inactive</span>}</td><td className="px-5 py-5 text-right"><Link href={`/admin/customers/${item.id}`} className="text-sm font-semibold text-blue-700">View</Link></td></tr>)}</tbody></table></div><div className="mt-4 grid gap-4 md:hidden">{result.items.map((item) => <article key={item.id} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-start justify-between gap-3"><h2 className="font-semibold text-slate-950">{item.name}</h2><span className={item.isActive ? "text-xs font-semibold text-emerald-700" : "text-xs font-semibold text-slate-500"}>{item.isActive ? "Active" : "Inactive"}</span></div><div className="mt-4"><Contact email={item.email} phone={item.phone} /></div><div className="mt-4 flex gap-4 border-t border-slate-100 pt-4 text-sm text-slate-600"><span>{item.propertyCount} properties</span><span>{item.requestCount} requests</span></div><Link href={`/admin/customers/${item.id}`} className="mt-4 inline-block text-sm font-semibold text-blue-700">View customer →</Link></article>)}</div><div className="mt-6 flex items-center justify-between"><span className="text-sm text-slate-500">Page {result.pagination.page} of {result.pagination.totalPages}</span><div className="flex gap-2">{result.pagination.page > 1 ? <Link href={pageHref(query, result.pagination.page - 1)} className="rounded-lg border border-slate-300 px-3 py-2 text-sm font-semibold">Previous</Link> : null}{result.pagination.page < result.pagination.totalPages ? <Link href={pageHref(query, result.pagination.page + 1)} className="rounded-lg border border-slate-300 px-3 py-2 text-sm font-semibold">Next</Link> : null}</div></div></>}</main>; }
