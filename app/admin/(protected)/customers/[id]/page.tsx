import Link from "next/link";
export default async function CustomerPlaceholder({ params, searchParams }: { params: Promise<{ id: string }>; searchParams: Promise<{ created?: string }> }) { const { id } = await params; const { created } = await searchParams; return <main className="mx-auto max-w-3xl"><Link href="/admin/customers" className="text-sm font-semibold text-blue-700">← Customers</Link>{created ? <p className="mt-6 rounded-xl border border-emerald-200 bg-emerald-50 p-4 text-sm font-semibold text-emerald-900">Customer added.</p> : null}<section className="mt-6 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><p className="text-sm font-bold uppercase tracking-[0.16em] text-blue-700">Customer</p><h1 className="mt-3 text-3xl font-semibold text-slate-950">Customer saved</h1><p className="mt-3 text-slate-600">Customer details and saved properties will be available here in the next customer workspace update.</p><p className="mt-4 text-xs text-slate-400">Reference: {id}</p></section></main>; }
