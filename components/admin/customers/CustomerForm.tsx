"use client";

import Link from "next/link";
import { useRouter } from "next/navigation";
import { useState, useTransition } from "react";
import { createCustomerAction } from "@/app/actions/create-customer";

const propertyTypes = ["HOUSE", "APARTMENT", "OFFICE", "COMMERCIAL", "AIRBNB", "OTHER"] as const;
type Form = { name: string; email: string; phone: string; addProperty: boolean; label: string; propertyType: string; addressLine1: string; addressLine2: string; city: string; state: string; postalCode: string; bedrooms: string; bathrooms: string; approximateSquareFeet: string };
const initial: Form = { name: "", email: "", phone: "", addProperty: false, label: "", propertyType: "HOUSE", addressLine1: "", addressLine2: "", city: "", state: "", postalCode: "", bedrooms: "", bathrooms: "", approximateSquareFeet: "" };

export default function CustomerForm() {
  const router = useRouter(); const [form, setForm] = useState(initial); const [duplicate, setDuplicate] = useState(false); const [error, setError] = useState<string | null>(null); const [pending, startTransition] = useTransition();
  const update = (key: keyof Form, value: string | boolean) => setForm((current) => ({ ...current, [key]: value }));
  const submit = (event: React.FormEvent) => { event.preventDefault(); setError(null); startTransition(async () => { const result = await createCustomerAction({ name: form.name, email: form.email, phone: form.phone, allowDuplicateContact: duplicate, property: form.addProperty ? { label: form.label, propertyType: form.propertyType, addressLine1: form.addressLine1, addressLine2: form.addressLine2, city: form.city, state: form.state, postalCode: form.postalCode, bedrooms: form.bedrooms ? Number(form.bedrooms) : null, bathrooms: form.bathrooms, approximateSquareFeet: form.approximateSquareFeet ? Number(form.approximateSquareFeet) : null } : null }); if (result.status === "SUCCESS") router.push(`/admin/customers/${result.customer.id}?created=1`); else if (result.status === "POSSIBLE_DUPLICATE") setDuplicate(true); else if (result.status === "INVALID_INPUT") setError(Object.values(result.fieldErrors).flat()[0] ?? "Check the form and try again."); else setError("We could not save this customer. Try again."); }); };
  return <form onSubmit={submit} className="space-y-6">
    {error ? <div role="alert" className="rounded-xl border border-rose-200 bg-rose-50 p-4 text-sm text-rose-900">{error}</div> : null}
    {duplicate ? <div role="alert" className="rounded-xl border border-amber-200 bg-amber-50 p-4 text-sm text-amber-950"><p className="font-semibold">A customer with this contact information may already exist.</p><p className="mt-1">Contact details are allowed to be shared, so you can deliberately create this customer anyway.</p><div className="mt-4 flex flex-wrap gap-3"><button type="submit" className="min-h-10 rounded-lg bg-amber-700 px-4 font-semibold text-white">Create anyway</button><Link href="/admin/customers" className="inline-flex min-h-10 items-center rounded-lg border border-amber-300 px-4 font-semibold text-amber-900">View customers</Link></div></div> : null}
    <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><h2 className="text-lg font-semibold text-slate-950">Customer contact</h2><div className="mt-5 grid gap-5 sm:grid-cols-2"><label className="text-sm font-semibold text-slate-700 sm:col-span-2">Name<input required value={form.name} onChange={(e) => update("name", e.target.value)} className="mt-2 min-h-11 w-full rounded-xl border border-slate-300 px-3 font-normal text-slate-950" /></label><label className="text-sm font-semibold text-slate-700">Phone<input value={form.phone} onChange={(e) => update("phone", e.target.value)} placeholder="(973) 555-1234" className="mt-2 min-h-11 w-full rounded-xl border border-slate-300 px-3 font-normal text-slate-950" /></label><label className="text-sm font-semibold text-slate-700">Email<input type="email" value={form.email} onChange={(e) => update("email", e.target.value)} className="mt-2 min-h-11 w-full rounded-xl border border-slate-300 px-3 font-normal text-slate-950" /></label></div><p className="mt-3 text-sm text-slate-500">At least a phone number or email address is required.</p></section>
    <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><label className="flex min-h-11 items-center gap-3 text-sm font-semibold text-slate-800"><input type="checkbox" checked={form.addProperty} onChange={(e) => update("addProperty", e.target.checked)} className="size-4 rounded border-slate-300" />Add a property now</label>{form.addProperty ? <div className="mt-5 grid gap-5 sm:grid-cols-2"><label className="text-sm font-semibold text-slate-700">Property label<input value={form.label} onChange={(e) => update("label", e.target.value)} placeholder="Home" className="mt-2 min-h-11 w-full rounded-xl border border-slate-300 px-3 font-normal" /></label><label className="text-sm font-semibold text-slate-700">Property type<select value={form.propertyType} onChange={(e) => update("propertyType", e.target.value)} className="mt-2 min-h-11 w-full rounded-xl border border-slate-300 px-3 font-normal">{propertyTypes.map((type) => <option key={type}>{type}</option>)}</select></label><label className="text-sm font-semibold text-slate-700 sm:col-span-2">Address<input required value={form.addressLine1} onChange={(e) => update("addressLine1", e.target.value)} className="mt-2 min-h-11 w-full rounded-xl border border-slate-300 px-3 font-normal" /></label><label className="text-sm font-semibold text-slate-700">Address line 2<input value={form.addressLine2} onChange={(e) => update("addressLine2", e.target.value)} className="mt-2 min-h-11 w-full rounded-xl border border-slate-300 px-3 font-normal" /></label><label className="text-sm font-semibold text-slate-700">City<input required value={form.city} onChange={(e) => update("city", e.target.value)} className="mt-2 min-h-11 w-full rounded-xl border border-slate-300 px-3 font-normal" /></label><label className="text-sm font-semibold text-slate-700">State<input required maxLength={2} value={form.state} onChange={(e) => update("state", e.target.value)} className="mt-2 min-h-11 w-full rounded-xl border border-slate-300 px-3 font-normal" /></label><label className="text-sm font-semibold text-slate-700">ZIP code<input required value={form.postalCode} onChange={(e) => update("postalCode", e.target.value)} className="mt-2 min-h-11 w-full rounded-xl border border-slate-300 px-3 font-normal" /></label><label className="text-sm font-semibold text-slate-700">Bedrooms<input type="number" min="1" value={form.bedrooms} onChange={(e) => update("bedrooms", e.target.value)} className="mt-2 min-h-11 w-full rounded-xl border border-slate-300 px-3 font-normal" /></label><label className="text-sm font-semibold text-slate-700">Bathrooms<input type="number" min="0.1" step="0.1" value={form.bathrooms} onChange={(e) => update("bathrooms", e.target.value)} className="mt-2 min-h-11 w-full rounded-xl border border-slate-300 px-3 font-normal" /></label><label className="text-sm font-semibold text-slate-700">Square feet<input type="number" min="1" value={form.approximateSquareFeet} onChange={(e) => update("approximateSquareFeet", e.target.value)} className="mt-2 min-h-11 w-full rounded-xl border border-slate-300 px-3 font-normal" /></label></div> : null}</section>
    <div className="flex flex-wrap gap-3"><button disabled={pending} className="min-h-11 rounded-xl bg-blue-700 px-5 font-semibold text-white disabled:bg-slate-300">{pending ? "Saving…" : duplicate ? "Create anyway" : "Add customer"}</button><Link href="/admin/customers" className="inline-flex min-h-11 items-center rounded-xl border border-slate-300 px-5 font-semibold text-slate-700">Cancel</Link></div>
  </form>;
}
