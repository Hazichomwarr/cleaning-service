import type { CleaningRequestStatus, PropertyType, WorkerType } from "../generated/prisma/client";

export type AdminWorkerDetail = { id: string; firstName: string; lastName: string; phone: string | null; email: string | null; type: WorkerType; isActive: boolean; currentAssignments: Array<{ id: string; requestId: string; requestNumber: string; serviceName: string; propertyType: PropertyType; status: CleaningRequestStatus; scheduledStart: string | null; scheduledEnd: string | null }>; recentAssignmentHistory: Array<{ id: string; action: "ASSIGNED" | "REMOVED"; requestId: string; requestNumber: string; changedAt: string; reason: string | null }>; createdAt: string; updatedAt: string };
type Row = { id: string; firstName: string; lastName: string; phone: string | null; email: string | null; type: WorkerType; isActive: boolean; createdAt: Date; updatedAt: Date; assignments: Array<{ id: string; assignedAt: Date; cleaningRequest: { id: string; requestNumber: string; propertyType: PropertyType; status: CleaningRequestStatus; scheduledStart: Date | null; scheduledEnd: Date | null; service: { name: string } } }>; assignmentHistory: Array<{ id: string; action: "ASSIGNED" | "REMOVED"; reason: string | null; createdAt: Date; cleaningRequest: { id: string; requestNumber: string } }> };
type Database = { worker: { findUnique: (args: Record<string, unknown>) => Promise<Row | null> } };
const select = { id: true, firstName: true, lastName: true, phone: true, email: true, type: true, isActive: true, createdAt: true, updatedAt: true, assignments: { orderBy: [{ assignedAt: "asc" }, { id: "asc" }], select: { id: true, assignedAt: true, cleaningRequest: { select: { id: true, requestNumber: true, propertyType: true, status: true, scheduledStart: true, scheduledEnd: true, service: { select: { name: true } } } } } }, assignmentHistory: { orderBy: [{ createdAt: "desc" }, { id: "desc" }], take: 10, select: { id: true, action: true, reason: true, createdAt: true, cleaningRequest: { select: { id: true, requestNumber: true } } } } };
export async function getAdminWorkerDetail(id: string, options: { database?: Database } = {}): Promise<AdminWorkerDetail | null> { const database = options.database ?? (await import("../lib/db/prisma")).prisma as unknown as Database; const row = await database.worker.findUnique({ where: { id }, select }); if (!row) return null; return { id: row.id, firstName: row.firstName, lastName: row.lastName, phone: row.phone, email: row.email, type: row.type, isActive: row.isActive, currentAssignments: row.assignments.map((item) => ({ id: item.id, requestId: item.cleaningRequest.id, requestNumber: item.cleaningRequest.requestNumber, serviceName: item.cleaningRequest.service.name, propertyType: item.cleaningRequest.propertyType, status: item.cleaningRequest.status, scheduledStart: item.cleaningRequest.scheduledStart?.toISOString() ?? null, scheduledEnd: item.cleaningRequest.scheduledEnd?.toISOString() ?? null })), recentAssignmentHistory: row.assignmentHistory.map((item) => ({ id: item.id, action: item.action, requestId: item.cleaningRequest.id, requestNumber: item.cleaningRequest.requestNumber, changedAt: item.createdAt.toISOString(), reason: item.reason })), createdAt: row.createdAt.toISOString(), updatedAt: row.updatedAt.toISOString() }; }
export { select as workerDetailSelect };
